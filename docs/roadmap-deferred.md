# ECDI 延期事项排期总表（roadmap-deferred）

> 状态：v1.29（2026-09-24）｜确认排期
> 作用：汇总全部"记账/延期/TODO/推迟"决策 → 对应实现阶段。README 技术债表的完整展开。
> ⚠️ **已知缺口（2026-09-11 声明）**：本表内容主体停留在 v1.1（2026-08-15），**9.6 / 9.7 / 9.8 / 10 / 11 / 12 的延期项尚未批量补记**（含 9.6 的 Fade→PushOpacity / DrawArc / PushTransform / DoubleClick / Dirty Region 等记账项）。v1.2 仅补登记抗锯齿一项（#29）——**不要把本表当作完整清单**。

## 1. Phase 7 平台抽象 + 测试体系（v1.0 转库前硬性前置；2026-08-15 裁决拆 7.1/7.2）

### 1.1 Phase 7.1 平台抽象

| # | 延期项 | 来源 | 备注 |
|---|---|---|---|
| 1 | Window 平台抽离（PlatformWindow + Backend 注入） | MEMORY.md 正式决策 | 主任务；Platform×Backend 二维独立 |
| 2 | 输入层抽象（TextInputInterface/TextInputContext） | README 技术债 | UpdateTextInputCaret 半抽象 → 契约层 + Win32IME adapter（与 Renderer→Backend 同构） |
| 3 | Imm/caret 平台代码下沉 | 5.6 文档 | NotifyIMEComposition 的 Imm → PlatformWindow::SetIMECompositionPosition；WindowMessageHandler → Win32PlatformWindow |
| 4 | 编辑操作可见性（TextBox 临时 public → 两层结构） | README 技术债 | 公开高层 API（InsertText/Clear/SetCaret）+ protected 底层原语 |
| 5 | Invalidate 两层结构（Internal + API 解耦） | README 技术债 | 批量编辑解耦 |
| 6 | 多窗口焦点语义评估（应用级 vs 窗口级，R3） | 架构回顾 | 全局快捷键/IME 全局态需要时 |
| 7 | 系统 caret 多窗口场景 | 5.6 记账 | 懒创建 + 单窗口单 caret 评估 |
| 8 | DPI 感知评估 | README 技术债 | 框架当前无 DPI 缩放，IME 坐标逻辑像素 |

### 1.2 Phase 7.2 测试体系

| # | 延期项 | 来源 | 备注 |
|---|---|---|---|
| 28 | 测试体系（单元测试框架 + 历史欠账补测） | 5.5.2 详细设计 P8（评审 要求②"不彻底放弃断言"） | 补 Selection 单元测试（选中 cd + 输入"中" → ab中e 等外部行为断言）；Phase 10 转库前测试保障 |

## 2. Phase 7.5 事件回调（裁决：插 7-8 之间）

| # | 延期项 | 来源 | 备注 |
|---|---|---|---|
| 9 | std::function 回调注册（Button::SetOnClick / CheckBox::SetOnCheckedChanged 等） | 用户 2026-08-15 | 继承 override 基座 + 回调便利层并存；Phase 7 解耦后做（稳定接口） |

## 3. Phase 8 渲染增强（能力层）

| # | 延期项 | 来源 | 备注 |
|---|---|---|---|
| 10 | 文本裁切裁剪区域（PushClip/clipRect 替代字符串截断） | TextBox TODO | O(n²) 截断 → 裁剪区域 |
| 11 | 焦点框虚线框 | 5.4 文档 | 现 DrawRect 两命令实线 |
| 12 | 渲染浮点化/亚像素 | 5.3/5.4 决策 | DrawTextContent int 参数等 |
| 13 | 6.2 CheckBox/Radio（勾/圆） | 用户 2026-08-15 | 消费 DrawLine/DrawRoundedRect；设计已定稿存档 |

## 4. Phase 8.5 文本系统 2.0（裁决：插 8-9 之间）

| # | 延期项 | 来源 | 备注 |
|---|---|---|---|
| 14 | IME 组合串内嵌（B 方案） | 5.6 文档 | 6 问题论证（组合串/Selection/Backspace/Delete/取消/候选切换） |
| 15 | 剪贴板子系统（Ctrl+A/C/V/X + Copy-Cut-Paste） | 5.5.2 推迟 | 评审 用户预期一致性（Ctrl+A 是剪贴板时代入口） |
| 16 | TextBox 多行/滚动 | 5.5 记账 | EnsureCaretVisible/自动滚动 |
| 17 | TextBox 双击/三击/选词/选行（m_dragSelecting） | 5.5.2 记账 | |
| 18 | 光标闪烁 | TextBox 记账 | m_showCaret 闪烁状态另立 |
| 19 | AutoSize / GetPreferredSize | 架构债务 | v0.1 不做（YAGNI），文本系统 2.0 评估 |
| 20 | Undo-Redo | 文本系统 2.0 | |
| 26 | SetFont()（TextWidget m_font 一行接入） | 5.1-5.3 预留 | 用户 2026-08-15 定入 8.5 |

## 5. Phase 9 主题系统（决策层）

| # | 延期项 | 来源 | 备注 |
|---|---|---|---|
| 27 | 主题系统（样式硬编码 → ApplyTheme/SetStyle） | 路线图 | 消费 Phase 8 能力 |

## 6. Phase 9.5 收尾补充（裁决：插 9-10 之间；原触发式项，v1.0 前择机完成）

| # | 延期项 | 来源 | 备注 |
|---|---|---|---|
| 21 | 局部更新/裁剪系统（Clip/Dirty Region/Partial Redraw） | Phase 4 决策 28 | 管线 Clip 已随 R1 落地（2026-08-28 重做）；Dirty Region 仍记账 |
| 22 | LinearLayout 抽象 | 6.1 契约 | ✅ 评估关闭（2026-08-28）：确认不抽——抽象后代码不减反增、第三用例未出现；第三布局立项时重启 |
| 23 | WM_MOVE 场景（移动中候选窗错位） | 5.6 记账 | ✅ 评估关闭（2026-08-28）：确认不做——模型 B 已消除主要错位（组合串自绘随窗）、场景极罕见（打字与拖窗互斥）、EXITSIZEMOVE 归位足够 |
| 24 | Hover / DoubleClick / MouseEnter / Leave | 5.4 架构债务 | ✅ R4 已实现（2026-08-27，commit 7d97d6c/0a11c2d）；DoubleClick 记账 |
| 25 | Shortcut System / 键盘入口统一 / InputManager | 架构回顾 R2 + 触发条件 | ✅ 评估关闭（2026-08-28）：三子项全部不做——键盘入口不对称=设计决策（Tab 导航只需 KeyDown）；无全局快捷键消费场景；InputManager 收益未证。重启：框架级快捷键消费场景出现 |

## 7. Phase 8.6 渲染抗锯齿（新立——2026-09-11）

| # | 延期项 | 来源 | 备注 |
|---|---|---|---|
| 29 | 抗锯齿升级（圆角/圆弧覆盖度） | `phase9.5-alpha-primitive-detailed-design.md` §4「明确不做（YAGNI）」 | ✅ **已实现（2026-09-11）**——三件套齐备（`docs/phase8.6-render-antialiasing-{requirements,preliminary-design,detailed-design}.md`，详设 v1.4），`ecdi_tests` **174/174 通过**（158 既有零回归 + 15 AA 新增 + 1 ModelProbe 回归）；**正式修订 9.5「约束 2（圆角无抗锯齿）」**——改为「alpha 合成与几何抗锯齿正交」；两层拆分（覆盖度生成 ⇄ 形状装配）+ 掩码按 radius 缓存 + `PatchSurface` 复用；公共 API 与全部控件零改动（消费方 Button/CheckBox/Panel/ProgressBar/Radio/TextBox/焦点框自动获益）。斜线（`DrawLine` AA）**另起**——`DrawLine` 连 alpha 路径都没有 |

## 7.5 Phase 15 滚动容器（新立——2026-09-17）

| # | 延期项 | 来源 | 备注 |
|---|---|---|---|
| 30 | **`HitTest` 父边界约束的通用化**（K4：子节点越界仍可命中，波及 hover/按下/移动/滚轮/拖入 5 条路径） | Phase 15 需求稿 §1.2 K4/K5 | Phase 15 以 **`ClipsChildren()` 门控**（需裁剪的容器声明式开启）局部收口，**不**通改全部容器命中语义（零回归风险无法在无消费者场景验证）。**已发现的真实实例（2026-09-18）**：`CaptionBar` 窗口宽 < 138px 时 `minButton.x < 0` 越界仍可命中（`CaptionBar.cpp:97`，全库无 `WM_GETMINMAXINFO`）——该缺陷**已经存在**，Phase 15 不处理。重启：第二个非滚动消费者（ListBox / 裁剪面板）出现时，把默认值从 false 翻成 true |
| 31 | **焦点滚入视口**（`PageUp/PageDown/Home/End` + Tab 聚焦时自动滚动） | Phase 15 需求稿 §4 | 与滚轮**驱动源不同**（焦点驱动 vs extent 驱动），未纳入 R1–R5。重启：ScrollView 内出现可聚焦控件集合时 |
| 32 | **平滑 / 惯性 / 弹性滚动** | Phase 15 需求稿 §4 | Phase 9.6 动画系统可支撑（`AnimationManager` per-Window tick），属体验增强 |
| 33 | **虚拟化（按需生成子控件）** | Phase 15 需求稿 §4 | 需内容模型抽象（数据源 → 可视行），YAGNI；大列表性能真实成为瓶颈时再评 |
| 34 | **TextBox 横向滚动条** | Phase 9.5 R1 详设 L102（v1.0 记账） | Phase 15 的 R2 只做**容器级**滚动条；TextBox 滚动是 **caret 驱动**（与 extent 驱动不同源）——若要给 TextBox 加条，须让 TextBox 内部视图接入 `ScrollBar`，是独立议题（Phase 15 §4 已显式划出） |
| 35 | **嵌套滚动停递**（内层滚到边界才向父级冒泡） | Phase 15 需求稿 D7（v1.1 降级） | 嵌套消费者**实测 = 0**（全库 `ScrollView` / `ScrollBar` 零实现）⇒ YAGNI。三种机制各有代价——① 改虚函数签名触及 5 个实现者（`Widget` / `TextBox` / `ModelListPanel` / `EventRouter` / `Application`）② Event 带 consumed **污染 Event 原则** ③ Application 认识容器（耦合）；代价表见 Phase 15 需求稿 D7。重启：出现第二个真实嵌套消费者 |
| 36 | **横向滚轮**（`WM_MOUSEHWHEEL`） | Phase 15 需求稿 K15/K16 · D8 | 平台层**未翻译** `WM_MOUSEHWHEEL`（`WindowMessageHandler.cpp:162` 只有 `WM_MOUSEWHEEL`）+ `MouseEvent` **无修饰键** ⇒ 横向只能靠滚动条拖动 + 显式 API。做须先扩平台翻译 + Event 维度（轴 / 修饰键） |
| 37 | **负向内容**（子控件坐标 < 0 的包围盒） | Phase 15 需求稿 D6 | extent 定义钳 0（`max(0, max(x + width))`）——布局系统（`VerticalLayout`）从 0 起算，负坐标不是正常布局产物 |

## 7.6 布局内边距（Layout padding）（新立——2026-09-19；阶段号待定，候选 Phase 17）

| # | 延期项 | 来源 | 备注 |
|---|---|---|---|
| 38 | **容器级内边距**（`LinearLayout` padding——一次性解决「窗口四周留白」） | 用户 2026-09-19 观察（ModelProbe 内容四边贴死客户区，鼠标拖选极易越出 GUI 边界） | **现状（已核实：零边距是结构性的，不是配置疏漏）**：`Window` 把 root 设为**客户区全尺寸**（`Window.cpp:444-445`）；`VerticalLayout::Arrange` 从 `y=0` 起算、**跨轴坐标恒 0**（`VerticalLayout.cpp:59`，注释「契约 4，现状不变」）、`fillCrossAxis` 时子宽 = **父宽**（`:53`/`:56`）、`remaining` 只减 `spacing` 不减任何 inset（`:37-38`）⇒ ModelProbe 的 13 个子控件各 680 宽、贴死左右与底边。**框架当前无任何容器级 padding/margin**（全库唯一 `padding` 是 `TextBoxStyle::padding`——控件内文字内边距，与容器无关；`Layout.h` 只有 `Arrange(Widget&)`）。**Phase 9.8 曾明确拒绝过**（需求 §4 非目标 + 初设 §3.2 冻结「不要为 AutoSize 顺手搞一套完整 Padding/Style 系统」）——但那是「**不要顺手搞**」，理由是**需求未出现**；**现在需求已出现**。**倾向方案 B（框架侧）**：`VerticalLayout` / `HorizontalLayout` 各加 `int padding = 0` 形参，`Arrange` 改 **4 处**（起点 `y = padding` · `remaining − 2·padding` · 跨轴宽 `− 2·padding` · `SetPosition(padding, y)`）——**root 一处即全局留白**（root 自身也是 `VerticalLayout`），ModelProbe 只需改两处构造参数（`main.cpp:262` + `ModelProbe.cpp:155`），**无需 demo 侧包裹容器**。**备选方案 A（仅 demo 侧）**：`ModelProbePage` 加内层容器 + override `SetSize` 同步几何（`SetSize` 是虚、`TextBox` 有 override 先例，`Widget.h:88`）——代价是绕开框架缺能力，此后每个想留白的 demo 都要重复这套同步。**⛔ 不得并入 Phase 16**（Phase 16 的定性就是「纯实现层 · 公共 API 净增 0」）。**触角提醒**：只要动 root 一级就会碰到 `main.cpp`（**须单独授权**——skill 条 2）。重启：**✅ 已立项并推进——Phase 17**（2026-09-20：方案 B 经用户拍板；需求 `phase17-layout-padding-requirements.md` **v1.1** ✅（回填同步）· 初设 `phase17-layout-padding-preliminary-design.md` **v1.1** ✅ · 详设 `phase17-layout-padding-detailed-design.md` **v1.1** ✅ **评审通过——可进入实现**）。**✅ 已实现——Phase 17**（2026-09-21 收口：批 A 源码 4 文件 +38/−26 · 批 B `LayoutTests.cpp` 505→**879** 行 / +8 用例 · A6 示例落盘；`ecdi_tests` **226 全绿** · 断言特征串 **10 → 11** · A1–A6 实测通过）。**★ 落地与计划的偏离**：留白最终落在 **page 一级**（`ModelProbe.cpp:155` = 12）而**非 root**——root 是裸 Widget（无背景），让出的边会露 Backend 白并连带内缩 `CaptionBar` ⇒ 派生新条目 **#39**（见 §7.7） |

## 7.7 根/窗口背景能力（新立——2026-09-21，由 #38 实施派生；**已立项 Phase 18**）

| # | 延期项 | 来源 | 备注 |
|---|---|---|---|
| 39 | **根 / 窗口的背景色可配置**（或让根能挂 `Panel` 背景） | Phase 17 A6 实测（2026-09-21） | **现象（已实测确认）**：`root` 是裸 `Widget`（**无背景能力**），而 Backend 每帧以 `WHITE_BRUSH` 清屏（`GDIBackend.cpp:261` **决策 16**「Root 白底是平台语义，不是 Widget 命令」）⇒ **任何未被 Widget 覆盖的客户区像素 = 白色**。因此在 root 一级配 `padding` 虽能缩进全部内容，却让**四边露出白色**（`#0f1115` 深色窗口上一圈白框），且 `CaptionBar` 作为同布局的子会被**连带内缩**。**Phase 17 的处置 = 绕开**（留白放 page 一级）。**重启条件**：出现「**真·窗口四周留白**」（露底色那一种）的真实设计需求——Phase 16 的 Desktop 档窗口、DesktopNest 的框体都可能触发 ⇒ 届时前置即本条目。**重启：✅ 已立项——Phase 18**（2026-09-21）。**进展（2026-09-22）**：需求 **v1.1** ✅ · 初设 **v1.1** ✅ · 详设 **v1.1** ✅ **均评审通过（第二轮评审：🟢 可进入实现）——待实现**（`docs/phase18-window-background-{requirements,preliminary-design,detailed-design}.md`；**公共 API +2**、用例 226 → 231 为设计目标）。**✅ 已实现并收口（2026-09-22）**：四工具链 `ecdi_tests` **231 全绿**（226 + 5，**实测 = 设计目标**）· 断言特征串 **11 → 11**（未新增）· **公共 API +2**（`Window::SetBackgroundColor` + `Application::Create` 的 `services` 形参）· **A1–A7 全过**（A1/A2/A3 实测 · **A4/A5 源码级核验** · A6 目视 · A7 文档同步）· ★ **实现期勘误**：详设 △8 原写「6 处 `BeginFrame` 调用点」⇒ 实测 **3 处**（6 = Begin + End 合计；详设已回写 **v1.2**）· ★ **A6 目视结论改写了本条的"重启"判断**——root 级留白虽已能露底色，但**连带内缩 `CaptionBar` 且与平台拖动区脱钩** ⇒ 该形态**否决**，见 **#40** |

---

## 7.8 同一父布局下的留白归属（新立——2026-09-22，由 Phase 18 A6 实测派生；**已立项 Phase 18.1**）

| # | 延期项 | 来源 | 备注 |
|---|---|---|---|
| 40 | **留白只作用于内容、不作用于 chrome**（子节点级内缩 / 布局豁免 / 应用侧组装） | **Phase 18 A6 目视实测**（2026-09-22；用户原话：「底色和原有底色不同，标题栏能不能做到不内缩」） | **现象（已实测）**：`Layout::padding` 是**布局级单一 int**、**对全部子一视同仁**（`VerticalLayout.cpp:38` · `:42` · `:45` · `:63`，`HorizontalLayout.cpp` 逐行同构），而 `CaptionBar` 与 `page` **同属 root 布局的子**（`main.cpp:280` / `:298`）⇒ root 级留白必然**连带内缩标题栏**。**Phase 18 只解掉了一半**：「露出来的是不是白的」已解决（清屏改用本帧背景色，`GDIBackend.cpp:260-266`），「标题栏跟着走」**未**解决。**A6 的临时处置** = 留白下沉到 page 一级（`ModelProbe.cpp:155`），代价 = **留白处看不见窗口底色**（page 自绘背景遮住）。**★ 关键事实（决定本项可"不开"）**：`Panel` 默认**背景透明**、官方语义即「**隐形布局容器**」（`DefaultTheme.cpp:44-47` / `Panel.cpp:78`）⇒ **应用侧包一层透明 `Panel` 承接 padding，零框架改动即可达成同一视觉**；Phase 17 判该路线代价过高时所依据的「需 override `SetSize` 同步几何」**已因 9.7 + Phase 15 而消失** ⇒ 该理由权重须重估。**重启：✅ 已立项——Phase 18.1**（2026-09-22 用户拍板「开一个小阶段」）——需求稿 **`docs/phase18.1-child-inset-requirements.md` v1.0** ⏳ **待评审**（路线 **A** 子节点 inset〔倾向〕/ **B** 布局豁免 / **C** 应用侧零改动〔可立即验证〕/ **D** 不做；**评审第一件事是判 D0**）。**✅ 已就地解决（2026-09-22）——Phase 18.1 搁置，结论四条**：① ★ **要求分三层**：**chrome 贴边全宽 = 基本要求（平台契约）** · **内容四周留白 = 基本要求** · **留白处显示窗口底色 = 可选效果**——**前两条在 `af1f080` 已成**，本条目真正待解的只有第三条。② ★ **「chrome 贴边」是平台契约、不是审美偏好**：`Win32PlatformWindow.cpp:417` 的 `WM_NCHITTEST` 用 `y < captionHeight` 的**全宽顶部带**判 `HTCAPTION`（拖动 / 双击最大化 / Aero Snap），**不看 `CaptionBar` 的实际几何** ⇒ chrome 一旦内缩，「**画出来的**」与「**能拖的**」脱钩：环上那块"背景"落在带内 ⇒ **能拖窗口**；标题栏**下缘 12px** 落在带外 ⇒ **拖不动**。③ **实现（路线 C）**：应用侧插一层**透明 `Panel`** 承接 `padding`（9.6 语义「隐形布局容器」，`a == 0` 时不产生绘制命令）⇒ **框架零改动**、公共头 **92 → 92**、用例 **231 → 231**；代价 = 每个窗口多一层组装节点 + **一条沉默的负约束**（内容自身的 `padding` 必须归零，否则两级叠成 24）。④ **路线 A（`Widget::SetInset` + `Layout` 基类唯一消费 helper）降级为记账**：它与 C **等价**（同样只能达成第三条、同样要求"内缩落在内容上、父 padding 归零"），且**多引入一个近义词**（`padding` vs `inset`）——与「**一个概念只用一个词**」的教学标尺冲突；★ **它并不能让「root 一级配 padding 就自动 chrome 贴边」**。**★ Phase 17 的 R3 措辞更正**：「root 一级即**全局**留白」这一表述里"全局**含 chrome**"的前提**是错的**，正确表述 = 「**root 的*内容区*留白，chrome 不受影响**」。**重启条件（任一命中即重启；届时优先 B/E，而非 A）**：**R-1** 出现**第二个**窗口 / 场景需要同款「贴边 + 留白 + 露底色」⇒ 组装样板从"一次"变"每次"；**R-2** 需要在**同一个父**下给**不同子**不同内缩（C 的手法会退化成"每个兄弟包一层"）；**R-3** 这条规则需要**测试守卫**（现状：C 的约定**无用例可拦**——`Panel` 若改默认不透明、内容若又加回 `padding`，测试全绿而界面悄悄坏掉）；**R-4** 要求「**在 root 配 `padding` 就自动 chrome 贴边**」⇒ 只有 **B（布局豁免）** 或 **E（chrome 由 Window 管）** 能解，**A 也解不了** |

## 7.9 框架缺陷修复队列（新立——2026-09-22；**审计见 `docs/framework-defect-audit.md`**）

> 本节只做**条目登记**（与全项目延期项同表管理）。**判据 / 证据 / 排序理由 / 记账不动的理由**一律在审计文档，**不在此复述**——避免两处各说一套。

**背景**：用户 2026-09-22 拍板「**先做缺陷修复的优先级更高**」——desktop demo 定位是「功能的用法示范」而非开发脚手架，**推迟到框架缺失补齐之后**；ModelProbe 继续承担「第一个真实消费者 + 回归载体」。

| # | 项 | 类型 | 顺位 | 备注 |
|---|---|---|---|---|
| **41** | **鼠标事件维度补全**——`MouseEvent` 家族补「**当前按下的按键位 + 修饰键**」（`WM_MOUSEMOVE` 的 `wParam` 现被翻译器丢弃：`MouseEvent.h:41-44` 仅 x/y · `WindowMessageHandler.cpp:92-106` 只取坐标） | **契约错误（丢信息）** | **①** | **✅ 已实现并收口——Phase 19**（2026-09-23；四工具链 **236** 全绿 · 断言特征串 **11→11** · 公共 API **+2** · **8 文件改动 / 无新文件**）。三件套均评审通过：需求 v1.1 / 初设 v1.1 / 详设 v1.1（`docs/phase19-mouse-event-dimensions-*`）。★ **顺带解锁 #36**（横向滚轮卡在"无修饰键 / 轴"）；★ 现有两处手工状态特例（`ScrollBar::m_dragging` / `TextBox::m_mouseDown`）**不在本项范围内**（它们另有捕获 / 拖选职责）· **✅ 需求 v1.1 + 初设 v1.1 均评审通过（2026-09-23）**（初设：`docs/phase19-mouse-event-dimensions-preliminary-design.md`）· ★ **重启条件（2026-09-23 初设 v1.1 立）**：**①** 出现**公共消费者**需**手工构造带按键状态的 `MouseEvent`**（测试替身 / 合成事件注入）⇒ 才引入命名类型（`MouseButtonMask` 或等价物；届时靠默认值 `0` **零破坏**）。**②** 出现**真实消费者需要 Alt + 鼠标**（如 Alt 拖复制）⇒ **只改 Win32 翻译器**（`TranslateMouseModifiers` 补 `GetKeyState(VK_MENU)` 一位，并注明"该位为**查询时刻**"）；★ **Event 模型本就支持 `KeyModifier::Alt`**——缺的是**当前 Win32 映射**，**不要**写成"MouseEvent 不支持 Alt" |
| **8（复用）** | **DPI 感知**（系统级 → Per-Monitor V2；并补**屏幕 / DPI 查询 API**） | **真缺陷**（无替代路径） | ② | **不新开号**——本条即 §1.1 条 **8**「DPI 感知评估」的**优先级提升**；证据补 Phase 16 spike 实测跨屏 reparent 后矩形 ×0.8（`desktopnest-roadmap.md:247`）。⚠️ **触面广**（坐标换算点散布）⇒ 先定 API 形态再动 · **✅ 已立项——Phase 20**（2026-09-24）· 需求稿 `docs/phase20-dpi-awareness-requirements.md`（**✅ v1.1 已通过**——外部评审第一轮判定「需求边界成立，Q1–Q6 已正确归类为初设决策 ⇒ 可进初步设计」；★ v1.1 为**核实补充 + 论证加固**（方向未变）：**新增 K13「客户区尺寸 3 个入口」**（v1.0 只覆盖事件那一路，漏了 `OnResized`——**布局几何的真正源头**）· **§4.1「同一 `GetClientRect` 两处两种单位」** · **R3 升为架构约束** · **Q5 增倾向 A**）· ★ **初步设计 v1.0 待评审（2026-09-24）**——`docs/phase20-dpi-awareness-preliminary-design.md`（回答需求 **Q1–Q6**：六项定案；★ **本稿新勘四条关键事实** = **两处字体缓存**（`GDIBackend.h:76` / `GDITextMeasurer.h:32`，键均不含 DPI）· **测量器用屏幕 DC**（`GDITextMeasurer.h:16`，而渲染用窗口 DC）· **`TextMeasurer` 接口无 DPI 参数** · **翻译器无 DPI 来源**（`WindowMessageHandler.h:32`））· ★ **初设 v1.1（评审通过 2026-09-24，无阻塞项）**：★ **评审指定详设必做三件** = **O3 `PixelsToDip` 舍入**（★ 双向恒等**数学上不可能** ⇒ **不得当 bug 去修**）· **O6 `WM_DPICHANGED` 链**（★ **必须接入既有 `WM_SIZE → OnResized → layout` 链，避免两套 resize 路径**）· **O4 声明失败策略** ⇒ ★★ 已成 **§2.3.1「读取而非假定」**（框架从不假定 V2 生效、**始终读当下 DPI** ⇒ 三条路径各自自洽，**不存在半降级**）；§9 **六件事 → 七件事**（新增 ⑦）· ★ **详细设计 v1.0 待评审（2026-09-24）**——`docs/phase20-dpi-awareness-detailed-design.md`（**△1–△25 逐文件行级改动**；★ **本稿最实质改进 = `DipToPixels` 改纯函数**：现签名收 `HWND`，导致「纯换算」无法无头测试，与初设 §9-① 直接矛盾 ⇒ 改收 `int dpi`，同时达成"可测"与"DPI 来源/转换解耦"）· ★ **详设 v1.1 已通过（2026-09-24）**：★★ **补契约 C10（负数舍入）**——评审实测发现 half-up **对负数不成立**（C++ 整数除法**向 0 截断**；`dpi=144` 的 `-20..0` 区间**两函数各 20/20 全偏**）⇒ 两函数改**共用 `RoundHalfAwayFromZero`**（`sign(x)·floor(\|x\|+0.5)`，**远离零**），且**实测正数与旧式逐位等价**（不破坏 G5）；另修溢出措辞 + **冻结 Win10 1703+ 支持基线**· ★ **本项的特殊性**：**不是引入新概念，而是让既有契约生效**——DIP 契约早在 **Phase 13** 就已声明（`Win32PlatformWindow.cpp:376`），并留下 **L6**（`WM_NCHITTEST` 物理像素 vs widget DIP 几何）明写「与 DPI 感知一并闭合」（`phase13-captionbar-detailed-design.md:795/1051`） |
| **（引用）** | **`HICON` → `Image` 通路** | 缺失能力（接口形态已定） | ③ | **登记处 = `desktopnest-roadmap.md` §5 的 G-2**——**不在此另开号**（避免三份账）。它是 desktop **唯一阻断项** |
| **（引用）** | **工作线程 → UI 线程投递** | 缺失能力 | ④ | **登记处 = 同上 G-3**。有兜底（`TimerEvent` 轮询）⇒ 可等 |
| **42** | **列表 / 网格容器**（`ListBox` / `ListView` / `TreeView`） | 缺失能力（**第二个消费者已出现**） | ⑤ | **新立**（2026-09-22）。★ 与 **#33（虚拟化）解耦**：先要"容器"，虚拟化是后一步；先例 = ModelProbe 的手工行池（`ModelProbe.cpp:715-753`） |
| **43** | **抗锯齿只覆盖圆角**——斜线 / 曲线边缘为硬边（`DrawLine` 走 GDI `MoveToEx`/`LineTo`）= 审计 **D-6** | **缺失能力**（★ 消费者**已存在且目视可见**：多选控件的勾） | ⑥ | **新立**（2026-09-23）。★ **与 #44 同源**：都是「**视觉语义（AA / 点线 / 圆角）属于后端私有实现，而非框架共享能力**」的侧面。**实现方向** = **覆盖度光栅化的扩展**（沿用 `CornerCoverageMask` 的 pixel-square coverage 模型 —— 圆盘已实现 ⇒ 再加**线段覆盖度**即可）；★ **接缝已铺好**：`Render/CoverageRaster`（2026-09-23 新抽出的平台无关像素合成）就是它该落的地方 |
| **44** | **后端可替换性（形态记账）**——`RenderingBackend` 对"换后端"**部分就绪** | **形态记账**（非缺陷；**第二后端未出现**） | — | **新立**（2026-09-23）。**已就绪**：operation-level 粒度 · 资源描述符平台无关（`Font`/`Image`/`Color`）· 测量能力独立注入 · 上下文空基类（`PlatformRenderContext`）· ★ **`RecordingBackend` = 第二实现的实证** · ★ **覆盖度像素合成已抽出**（`Render/CoverageRaster`）。**四个缺口（v1.17 修正 — 原判 M-3 过重，见下）**：**M-1 渲染目标不可指定**（`BeginFrame(const Color&)` 无尺寸 / 无目标；GDI 靠 `GetClientRect`+`BeginPaint` 自省 ⇒ **离屏 / 导出 / 无窗口无处落笔**；★ **Android/EGL 下最明显**——`EGLSurface` 必须显式创建）· **M-2 无 resize 接缝**（GPU 后端须重建 swapchain / RT / `EGLSurface`；`OnTargetResized` 不存在）· ★ **~~M-3~~ 降级为前瞻项**：`EndFrame` = submit + present **对 GDI / GL 这类「画完即呈现」的模型是正确抽象**（GL 的 `SwapBuffers` / `eglSwapBuffers` 恰好落在这一位）⇒ **只有 Vulkan / D3D12 那种需要分离 submit 与 present 的后端才会暴露**；**从"缺口"降为"Vulkan 阶段的前瞻项"**（2026-09-23 用户给出后端路线图后修正）· ★★ **M-4 文本栅格化属后端职责**：GDI 靠 `TextOutW` **由系统栅格化**；GL / Vulkan 必须**自己栅格化字形 + 维护纹理图集** ⇒ **这是 GPU 后端面里最大的一块工作量**，且与 `TextMeasurer` **成对替换**（Phase 7.1 预留的 `OpenGLRenderer + FreeTypeTextMeasurer` 配对**方向正确**）。**非缺口但须知的代价（M-5）**：GPU 后端需要**攒批**（operation-level 逐个 draw call 会打断批次、纹理切换更甚）⇒ **接口不必改**（后端内部可累积到 `EndFrame` 一次提交，`RenderCommand` 序列本身有序 ✓），但**后端复杂度由此上升**（纹理图集 / 状态排序）。★ **重启条件（任一命中即重启）**：**R-1** 要做**离屏渲染 / 导出 / headless 测试**；**R-2** 要上 **GPU 后端**；**R-3** 要**共享 device 的多 target**（多窗口 / 分层 / 合成）；**R-4** 要**后端间视觉等价**（AA / 点线 / alpha 契约化）。★ **届时优先加的三个位**：① `virtual void OnTargetResized(int w, int h) {}`（**带默认空实现、GDI 零改动**）② `BeginFrame` 带目标尺寸（或引入 `RenderTarget`）③ 契约补三条视觉语义。★ **后端路线图（用户 2026-09-23 明确，原样记）**：**Windows = GDI（默认）** → **Linux + 未来大概率会做的 Android = OpenGL / OpenGL ES** → **等都能用了再开始学 Vulkan**。⇒ ★ **重启条件补 R-5**：**Linux / Android 适配开工时即重启本条**（不必等到"要 GPU"）。★ **选库权衡（两条并存，不互斥）**：若 Windows 侧也要 GPU ⇒ 直接上 **GL**（与 Android 同一套代码）；若只求 Linux/Android 的 2D 表现且**想少写 M-4**（字形栅格化 + 图集）⇒ **Cairo / Skia** 更省（自带 AA 与文本）。★ **建议的验证方式**：真写 GL 后端之前先做**空壳 spike**（只画矩形 + 文本，跑通 `BeginFrame` / `EndFrame` / `PushClip`）——**用实测代替纸上分析**（沿用 Phase 16 spike 的做法）。★ **Android 侧预告的额外成本（不在本条范围，但别漏记）**：Android **没有鼠标**（是**多点触控**）⇒ 事件模型的「单指针 + 按键集合」不够用；且**键码表要写第三份**（`AKEYCODE_*`）⇒ **Android 的适配大头在平台层与事件模型，不在渲染** |
| **45** | **3D 渲染扩展边界**——★ **核心决策：3D 不进入 ECDI Core Rendering Pipeline** | **架构边界记账**（非缺陷；**无 3D 消费者**） | **最低**（★ 用户 2026-09-23 口径：**半年～一年内不会有 3D 渲染需求**） | **新立**（2026-09-23，源自 backend 选型讨论 + 两轮外部评审）。★★ **为什么必须走旁路（证据链）**：`RenderCommand.h:90-91` 明写「新增命令类型必须同步给 Renderer 加 `ExecuteCommand` 重载——`std::visit` 穷尽性保证漏加即编译错误」⇒ 若加 `DrawMeshCommand`：`Renderer` **必须**加重载 ⇒ **必须**转发到 backend 的 3D 方法 ⇒ 该方法**必须**出现在 `RenderingBackend` 里 ⇒ 要么**纯虚**（GDI **被迫实现**）、要么**非纯虚默认空实现**（**静默丢弃**，且 `Renderer` 无从知道后端支不支持）⇒ **与「3D 可选」自相矛盾**。⇒ **决策**：`RenderCommand` / `PaintContext` / `CommandBuffer` / `Renderer` / `RenderingBackend` **全部不改**（一条很强的边界；等于「进 `RenderCommand` 者即成 Core Pipeline 的一部分」这一性质的直接推论）。★ **形态**：**独立可选接口**（名 **`RenderingBackend3D`** 为**候选**、**未定死**）——与 `RenderingBackend` **无继承关系**（★ 项目**没有** `Backend` 公共基类，**不要为它引入一个**）；支持者**多继承**两者（`GPUBackend : public RenderingBackend, public RenderingBackend3D`），`GDIBackend` **只继承前者** ⇒ **「2D 强制 / 3D 可选」字面成立 · 框架本体零改动 · GDI 一行不改**；探测走 **`dynamic_cast`**（**同层**先例已有：`Radio.cpp:35` 兄弟探测 · `RenderingBackend.h:35` 明写「**非跨层** dynamic_cast」）。★ **旁路必须被正式承认**：3D 是「**应用 ↔ 后端**」直连，**不经** `Widget → PaintContext → CommandBuffer → Renderer` ⇒ 属**第五条通路**，**须在文档显式声明**（评审原话：**坏的不是有旁路，坏的是没有承认它是旁路**）。★ **消费形态优先级（用户 2026-09-23 拍板）**：**① 3D 当「视图」**（GUI 内嵌 3D 格）**= 优先** · **② 3D 独占窗口 = 有需求时再考虑** · **③ 2D/3D 同帧合成 = 看情况**。★★ **但 ① 的接口形状不现在定死**（第二轮评审修正，**已采纳**）：`Image RenderToImage(scene, size)` 只是**一种可能**，**不作本条结论**；★ 其**隐藏限制**（已由 `Core/Image.h:10/21` 证实）：`Image` 是 **CPU 侧值语义**（`std::vector<std::uint8_t>`，自述「值语义（可拷贝进 `RenderCommand`）」）⇒ 若 ① **高频刷新**，会有 **GPU → readback → upload 往返 + 再拷一份**的代价 ⇒ **① 在静态 / 低频 3D 内容下性价比最高；高频全屏 3D 应走 ②**。★ **决策成本不是零**（评审修正，**已采纳**）：架构上「现在不改代码」成本确实低，**但将来一旦公开 `RenderingBackend3D`，其名字 / 生命周期 / 资源模型 / 线程模型 / Context 归属都会成为公共 API** ⇒ 故现在**连名字都只作候选记录**，**不视为已定**。★★ **重启时的第一问题**（**比「有哪些虚函数」更前置**）：**「3D 扩展是 ECDI 自己负责的能力，还是允许外部模块通过这个接口接入？」** ⇒ 答案直接决定 `RenderingBackend3D` 是**高层场景接口**（Scene / Camera / Mesh）还是**接近 GPU Resource / Command 的接口**（buffer / pipeline / 资源生命周期）。★ **命名纪律两条**：**无 `I` 前缀**（既有公共类型 `RenderingBackend` / `TextMeasurer` / `PlatformWindow` / `PlatformRenderContext` 均无）⇒ 应叫 `RenderingBackend3D`，**不是** `IRenderingBackend3D`；★ **`Viewport` 一词已被 2D 占用**（`ScrollView::ViewportWidth/Height`）⇒ 3D 侧若需「视口」概念**必须换词**（「一个概念只用一个词」）。★ **重启条件（任一命中）**：**R-1** 出现**真实 3D 消费者**且**形态 ① 已不足以满足**（要整窗 3D 或同帧合成）；**R-2** **#44 的 R-5**（Linux / Android 适配开工）**顺带触发**——GL / GLES 天生能画 3D，届时会自然面对「要不要顺手支持」。★ **重启前先回答「第一问题」**，再谈接口形状。 |

**记账不动的项**（`Panel::ContainsPoint` 恒 false · `~Application` 析构序 · `m_running` 语义 · `DrawText` 改名 · 光标色 · 键盘三入口 · 多窗口焦点 · AA 遗留验收）**不重复登记**——它们已有各自条目（#6 / #33 等）或归 **v1.0 API 审查**；逐条理由与重启条件见审计文档 **§7**。

---

## 8. 修订记录

- v1.29（2026-09-24）**Phase 20.1 初设升 v1.1（评审处置）+ 详细设计 v1.0 落盘（待评审）**。① **初设 v1.1**（评审结论「**基本通过，可以进入详细设计**」，零阻塞项）：★ **采纳两处文档修正**——**§2.5** 收紧「必然零回归」（数学恒等成立，但**不得外推成「247 个用例必然不变」**；改为**三层表述**：单值 / 几何字段 = **数学恒等（强）**，**整条链 = 工程判断、须实测**）· **§2.4 Q7** 把「≤1px」收成「**对正常有限几何值约 1px 量级**，**负数取整语义不在本子阶段定义**」；★ **评审确认保留 `Execute(commands, scale)` 形态**（并指出「帧级坐标变换」与 `Execute` 形参的**术语错位**）；★ **§9 五件事 → 七件事**（新增 ⑥ `scale` 语义冻结 · ⑦ 合法范围 = finite + positive）；★ **§7 T20.1-3 增补断言**（**源 `Image` 内容不变**）。② **详细设计 v1.0** 产出 `docs/phase20.1-render-dpi-scaling-detailed-design.md`（476 行 · 十一节）：★ **B12–B15 基线**（**B13 零 include 成本** / **B15 零容差新定义**）· ★★ **§1.3 修正 A：实测证伪初设 §9-③**——`scale = 1.5f` 与整数相乘**全部精确**（`1.5 = 3/2`，半整数亦然），真正非精确的是 `1.2f`/`1.1f`（`3 × 1.2f = 3.6000001`）；★ 并由此拿到**更好的测试设计**：**取奇数输入使期望值落在半整数上**，才能分辨「乘法」与「乘法后取整」（**全取偶数则抓不到偷偷取整**）。③ ★ **三项「详设必须冻结」全部落地**：**§3** `scale` 语义（**本次 Execute 的上下文参数**、Renderer 不保存、不依赖 `BeginFrame` 顺序、**不认识 DPI**）+ 合法范围 **finite + positive**、★ **不加运行时检查**（并记录"为何不加 `assert`"）· **§4** 零回归三层表述。④ **§2.5 实现盯防清单 6 条** · **§5 契约 C-7–C-11 验证映射 + A4 三条 grep 判据** · **§6 五用例完整输入/期望（float32 脚本复算）** · **§7 三批实现顺序**（批一 `Renderer.{h,cpp}` ⇒ 247 全绿 · 批二 测试 ⇒ 252 全绿 · 批三 `Window.cpp` 注入 ⇒ 四工具链 + 目视）。⑤ **影响面不变**：4 代码文件 · 公共头 92 → 92 · **用例 247 → 252** · **零 include / 零 `RunAllTests.*` / 零 CMake**。
- v1.28（2026-09-24）**Phase 20.1 初步设计 v1.0 落盘（待评审）**。① **产出** `docs/phase20.1-render-dpi-scaling-preliminary-design.md`（288 行 · 十节模板）。② ★★ **七项定案**：**Q1 落点 = `Renderer` 入口 + 形态 A2**（执行期**瞬时转换**、**不碰 `CommandBuffer`**；方案 B「后端逐方法」与 C「GDI world transform」**否决**——★ C 的致命点是 `AlphaBlend` **不受 world transform 影响** ⇒ AA 路径与 `DrawImage` 会与普通绘制不一致）；★ **Q2 定案 `Execute(commands, scale = 1.0f)`**（★ **与需求稿倾向的 `BeginFrame` 相反**——理由是 `BeginFrame` 的「单行转发、不存状态」契约已被 Phase 18 详设 §6 冻结；备选约 3 行可切换）；Q3「认识 `scale` ≠ 认识 DPI」· Q4 三个 helper（不加 `operator*`）· Q5 只折几何、不折 `font.size` · Q6 无头可测 · Q7 取整口径 = **既有近似**。③ ★ **两条新论证**：**§2.5** 用 IEEE-754 说明 `×1.0f` **精确** ⇒ **G2 是结构性保证、无需 `scale == 1` 特判**；**§2.1** 用「命令持有 `std::string` / `Image`」证明「复制命令再折算」不可行（**性能理由**，与 A2 的语义理由并列）。④ ★ **基线勘察勘误需求稿两条**（需求稿据此升 **v1.2**）：**C-6 的调用点计数 4 → 3**（实测 `Window.cpp:136` · `RendererTests.cpp:40` · `:91`）· **§7「公共 API = 0」→「0 新增头 / 1 处带默认值的形参扩展」**（`Renderer.h` 位于 `include/ECDI/`、**是公共头**）。⑤ **影响面**：**4 代码文件**（`Renderer.h/.cpp` · `Window.cpp` · `RendererTests.cpp`）+ 1 文档；**公共头 92 → 92**；**用例 247 → 252**（5 条，全部无头，扩展**既有** `RendererTests.cpp` ⇒ `RunAllTests.*` 零改动）；**后端零改动**（它们**看到的单位变了**，但接口与实现不动）。⑥ ★ **新增 O1**：后端取整口径不统一（`DrawRect` **截断** vs `PushClip`/`DrawLine` 用 **`lround`**）是**既有** ≤1px 近似 ⇒ 本子阶段不动（属 N5），**建议记账**。⑦ **C-7–C-11 新契约**（**延续需求稿的 C- 序列**，一个子阶段一套连续编号）。
- v1.27（2026-09-24）**Phase 20.1 需求稿升 v1.1（评审第一轮处置）**。① **评审结论**：外部评审「**通过，可以进入 Preliminary Design**」，**零阻塞项**——★ 评价本稿「**修正了 Phase 20 的单边界模型**」，并确认「`RenderCommand` 是框架内部 DIP 语义的**最后一个载体**」。② ★★ **采纳的实质补充（原稿缺）**：**A1 / A2 之分**（★ 必须**执行期间瞬时转换**、**禁止修改 `CommandBuffer`**——否则「绘制描述」变成「执行后被物理化的命令」，多后端共用同一缓冲会**语义污染**） · **`RecordingBackend` 不得承担缩放**（后端无论真假须看到**同一单位**，否则「测试通过而架构已坏」） · **单位不变式 U1–U5**（新增 **§3.1**：`PaintContext`→DIP · `Renderer` 输入 DIP · **输出给 Backend = 物理** · Backend **不再转换**）。③ **论证加固**：**C-1 理由澄清**（`DipToPixels` 是**平台 int↔int 契约**，与渲染层 `float×scale` **不是同一个数学接口**） · **C-4 补「不得提前 `round` / 截断」**（量化是绘图 API 的最后一步） · **C-5 展开为「只改数值，不改顺序 / 类型 / 数量」**。④ ★ **我方补充（评审未提）**：**新增 C-6**（`scale` 形参**必须带默认值 `1.0f`**——由**实测调用点**支撑：`Execute` **4 个** / `BeginFrame` **1 个**） · **新增 Q7**（`PushClip` 的 float vs 后端整数口径） · ★ **运行时自动生效**（scale 每帧取 `Window::GetDpiScale()` ⇒ 跨屏后**下一帧自动跟随**）。⑤ **新增 §8.1 元教训**：「**不要只问哪个模块知道平台，还要问哪个模块第一次要求物理单位**」——★ 转换落点由**单位契约**决定，不由目录名决定。⑥ **测试细化**：新增 **A6 / A7**（顺序 / 类型 / 数量不变 + 覆盖每种几何字段，★ `width` / `cornerRadius` 最易漏）。⑦ **结论**：**Q1 收敛 = `Renderer` 入口 + A2 形态**；**方案 B / C 明确否决**（B 会把坐标转换泄漏到每个后端；C 依赖 GDI 且不可复用）。⑧ ★ **顺带修正索引疏漏**：`docs/README.md` 的 **Phase 20 行**此前仍写着「需求确认 v1.0 · 待评审」（**漏改**）⇒ 已更正为「**需求 v1.1 ✅ · 初设 v1.1 ✅ · 详设 v1.1 ✅ · 五批实现完毕 + 崩溃修复**」。
- v1.26（2026-09-24）**新立子阶段 Phase 20.1（渲染层 DPI 缩放）—— Phase 20 的缺口补完**。① **来源**：Phase 20 详设 **§14 实施记录 R1**（2026-09-24 用户跨屏 / 主屏 125% 实测发现内容缩小约 1/3）+ **用户拍板「新开子阶段」**。② ★★ **根因 = 设计表述缺口（非实现笔误）**：初设 **§2.1 规则 2**（「`Renderer` / `RenderingBackend` 里见到的恒为**物理像素**」）与 **规则 3**（「两者**只在平台边界**那一条线上互换」）**互相打架**——渲染层**不在平台边界** ⇒ 「**框架内部(DIP) → 渲染层(物理)**」**这条边没有落点**。③ **实测证据**：`Renderer.cpp`（69 行）· `PaintContext.cpp`（84 行）**零 `Dpi`/`scale` 代码**；`GDIBackend` 直接把几何当像素（`static_cast<LONG>(rect.x)`）；全库 `src/Render/` 的 DPI 代码**只在字体路径**（批五）⇒ **字体已按 DPI 换算而几何未换算 ⇒ 尺度失调**（这是「控件框与文字比例不对」的来源）。④ **产出** `docs/phase20.1-render-dpi-scaling-requirements.md`（**需求确认 v1.0 · 待评审**）：**K1–K4**（证据）· **G1–G4 / N1–N5** · **约束 C-1..C-5** · ★ **§4 折算量清单**（8 类渲染命令逐条列明哪些量要折算；★ 明确 **`Font::size` 不在其列**——字号在后端换算，批五已做） · **Q1–Q6**（★ **Q1 换算落点** · **Q3 `Renderer` 是否允许认识 scale** · **Q6 无头可测性**） · **A1–A5**。⑤ ★★ **关键约束（写进需求，供初设遵守）**：**契约 C2** 禁止渲染层调用 `DipToPixels` / `PixelsToDip`；**渲染几何是 `float`** ⇒ **不适用 C10 的整数对称舍入**（C10 只服务平台层 `int` 几何：窗口尺寸 / 鼠标坐标 / IME）。⑥ ★ **对 Phase 20 的影响**：**A5（125%/150% 视觉）/ A6（跨屏）验收依赖本子阶段** ⇒ 本项收口前 **Phase 20 不得标「全部验收通过」**。
- v1.25（2026-09-24）**Phase 20 详细设计升 v1.1（评审第一轮处置）**。① **评审结论**：外部评审「**总体通过，可进入实现阶段**」，附 **1 处实施前必修 + 2 处文档精确性**。② ★★ **采纳评审的实质发现（实测确认，且比其举例更普遍）**：`DipToPixels` / `PixelsToDip` 的 half-up **对负数不成立**——C++ 整数除法**向 0 截断**，而 `+ 48` / `+ dpi/2` 的偏移是按「向 +∞」设计的。★ 实测：`PixelsToDip(-1,144)` 旧得 **0** / 应 **-1**；`DipToPixels(-1,144)` 旧得 **-1** / 应 **-2**；且 `dpi = 144` 的 `-20..0` 区间**两函数各 20/20 全偏** ⇒ **系统性错误**。③ ★★ **修正**：两函数改**共用 `RoundHalfAwayFromZero`**（语义 = sign(x) · floor(abs(x) + 0.5)，**远离零**），并**实测验证正数上与旧式逐位等价**（`0..2000 × 6 种 dpi` 差异 **0**）⇒ **不破坏 G5**（★ 这一步评审未验，是**采纳的前提**）。④ **新增契约 C10** + **§3.2.1 负数算例与来源定位**（★ **负数只可能来自鼠标坐标**——`WM_NCHITTEST` 的窗口坐标与 `WM_MOUSEMOVE` 的有符号坐标；**评审未提**）+ **§3.3 误差界补负数行** + **T20-6 负数用例**（原 T20-6..T20-10 **顺延为 T20-7..T20-11**）⇒ 场景数 **10 → 11**。⑤ **采纳处置 2** → **新增 §3.6**（溢出措辞精确化为「**防止中间乘法溢出**」，不暗示全范围饱和）。⑥ **采纳处置 3** → **§5.3 重写**（支持基线 **Windows 10 1703+** **冻结**，不再半悬；★ 属项目级支持范围决策，**待用户确认**）。⑦ ★ **自查抓到一处自伤**：表格单元格内的**绝对值符号**（`\|x\|` 这种写法）会被 Markdown 当作**列分隔符** ⇒ 3 处表格列数错乱，已按标准转义修正（★ 已归入 skill 条 29③ 的同族纪律）。⑧ **内容零删改**：B19–B28 · △1–△25 · C1–C9 · 五批顺序 · §4 链 · P1–P4 **全部保留**。⑨ 头部 v1.24 → **v1.25**。
- v1.24（2026-09-24）**Phase 20 详细设计 v1.0 落地**。① **产出** `docs/phase20-dpi-awareness-detailed-design.md`（**406 行 / CRLF + BOM**，十三节）。② **落实初设 §9 的七件事**（★ 评审指定三件均已成章：**§3 指定 ① `PixelsToDip` 规格** · **§4 指定 ② `WM_DPICHANGED` 链** · **§5 指定 ③ 声明失败策略**）。③ ★★ **本稿最实质的设计改进（对初设修3）**：**`DipToPixels` 改纯函数**——现签名 `(int, HWND)` 把"取 DPI"与"算公式"耦合 ⇒ **无头测试拿不到 `HWND`**，纯换算根本测不了（与初设 §9-① 直接矛盾）；改为 `(int dip, int dpi)` 并**移入 public 区**（沿用 `ResolveTarget` 的"public static 唯一目的是可被自动化测试覆盖"先例）⇒ 一举达成「**纯换算可无头测**」+「**DPI 来源与转换解耦**」；代价 = 那 2 个既有调用点（`WM_NCHITTEST` 的 `:377/:379`）**本来就要改**（Q5 定案 A 会让它们消失）⇒ **零额外成本**。④ ★ **对初设另三处修正**：`GDIBackend::GetOrCreateFont` **是实例方法**（初设记错为 static）· **IME 触面 3 处**（初设只写 1 处，漏了 `CreateCaret` 的**尺寸** ⇒ 150% 下光标不缩放）· `PlatformWindow::GetDpiScale` 纯虚的**实现者清单已 grep 确认 = 3 处**（1 生产 + 2 测试替身）。⑤ ★★ **§3.2 给出「双向恒等不可能」的纯整数反例**（评审给的是浮点算例；本稿用实际整数实现：`dpi=120` 时 `PixelsToDip(2)=2` 而 `DipToPixels(2)=3`）⇒ 新增 **契约 C9**：**两个方向各自保精度，但不得追求往返恒等**（详设/实现/评审均**不得当 bug 去修**）。⑥ ★ **§4 的硬约束**：`WM_DPICHANGED` **只做三件事**（更新 DPI → 采纳建议矩形 → 让系统发 `WM_SIZE`），**绝不自行调 `OnResized`**——否则形成**两套 resize 路径**（评审点名的风险）。⑦ ★ 新勘基线 **B19–B28**（含 **B24：既有 T4 用例天然是 G5 的回归锚**——`WM_SIZE 300×200` 逐位相等，Phase 20 后**必须原样通过**）。⑧ **△1–△25** · **C1–C9** · **T20-1..T20-10** · **五批实现顺序**（★ 批三的关键：先让"DPI 注得进去、事件换算得出"可验证，再动平台换算点）。⑨ **索引回写**：根 `README.md` 状态行 · `docs/README.md` Phase20 标题与文档表 · 本文件 #8 行。⑩ 头部 v1.23 → **v1.24**。
- v1.23（2026-09-24）**Phase 20 初步设计升 v1.1（评审通过）**。① **评审结论**：外部评审第一轮 ✅ **通过、可进入 Detailed Design、无阻塞项**；逐条处置见初设 **§1.4**。② ★★ **采纳评审最实质的一处批评 → 初设新增 §2.3.1「失败后继续到底意味着什么」**：定案 = **框架的行为是「读取」而非「假定」**——**从不假定"V2 已生效"**，而是**始终从平台读当下 DPI** ⇒ **三条路径各自自洽**（**成功** ⇒ 真实 per-monitor 值 · **失败且 unaware** ⇒ **恒 96 ⇒ 换算恒等 ⇒ 自动退化为现状路径**，**是 G5 护栏而非"半降级"** · **失败但应用已设别级** ⇒ 读到多少用多少）。这正面回答了评审担心的「**声明失败但实现仍无条件假定 V2 已生效**」。③ **O3**：**全盘采纳**评审的数学论断（**双向恒等在数学上不可能**，反例 `dpi=144` 时 `DipToPixels(1)=2` vs `PixelsToDip(2)=1.333…`）⇒ 详设**不得当 bug 去修**；★ **不采纳其"保留 float"的形态建议**（框架几何全是 `int`，消费点立刻截断 ⇒ float 只把**舍入点后移**）；**倾向整数 half-up 与正向对称**。④ **O6**：采纳评审的硬约束——**必须接入既有 `WM_SIZE → OnResized → layout` 链**（B5 ②），**避免两套 resize 路径**。⑤ **O4**：采纳「不得半降级」约束，并**倾向"V2 声明失败即不再尝试低级别 API"**（主动降级反而更差）+ `Log` 记录实际感知级别。⑥ **§9 六件事 → 七件事**（新增 **⑦ 感知声明失败策略**；★ 标注评审指定三件 = ①/②/⑦）。⑦ ★ **我方补充 P4**（"应用已设低级别感知"时 `GetDpiForWindow` 的返回值）——§2.3.1 第三条路径的**实测缺口**（评审未提）。⑧ **设计内容零删改**（六项定案 / C1–C8 / T20-1..T20-8 / A1–A6 / B1–B18 全部原样保留）。⑨ 头部 v1.22 → **v1.23**。
- v1.22（2026-09-24）**Phase 20 初步设计 v1.0 落地**。① **产出** `docs/phase20-dpi-awareness-preliminary-design.md`（**356 行 / CRLF + BOM**，十节模板）——回答需求 §8 的 **Q1–Q6**，把 **D0–D8 / 路线 B** 从"倾向"转为**定案**。② **六项定案**：**Q1** 单位分层（一个量的单位由「它跨越哪条线」决定；★ 唯一例外 = `GetClientSize()` 返回值 DIP 而实现内部物理）· ★ **Q2 文本测量返回 DIP**（含**自洽性论证**：测量链路自己进自己出用同一 DPI ⇒ 返回值对基准 DPI 近似不敏感；三条代价已记账）· ★ **Q3 感知声明落 `Application` 构造函数**（比任何 `Create` 都早 + 零应用改动；manifest 片段附带）· ★ **Q4 只加 `Window::GetDpiScale()`（float）**并**收敛 G4 范围**（屏幕枚举不做、记账）· ★ **Q5 定案 A**（入参换成 DIP；判据 = `HitTest` 入参语义恒为 DIP，**非**"符合 R3"）· ★ **Q6 DPI 为翻译器正常成员状态**（`SetDpi`），非测试缝。③ ★★ **本稿新勘出的四条关键事实**（需求稿未覆盖，`§1.2 B6/B8/B9/B15`）：**两处字体缓存**（`GDIBackend.h:76` + `GDITextMeasurer.h:32`，键均为 `(size, family)` **不含 DPI**）· **`GDITextMeasurer` 用 `GetDC(NULL)` 屏幕 DC 而渲染用窗口 DC**（`GDITextMeasurer.h:16`）· **`TextMeasurer` 接口无 hwnd/DPI 参数**（`TextMeasurer.h:19/22`）· **`WindowMessageHandler` 构造只收 host、无 DPI 来源**（`WindowMessageHandler.h:32`）。④ **契约 C1–C8**（★ **C2 = 换算只在平台边界、grep 可检**；**C5 = 零破坏精确口径**）· **O1–O7** · **T20-1..T20-8** · 验收 A1–A6 落地 · §9 交给详设的**六件事**。⑤ **索引回写**：根 `README.md` 状态行 · `docs/README.md` Phase20 标题与文档表 · 本文件 #8 行。⑥ 头部 v1.21 → **v1.22**。
- v1.21（2026-09-24）**Phase 20 需求稿升 v1.1（外部评审第一轮处置）**。① **评审结论：需求确认通过，可进初步设计**——Q1–Q6 已正确归类为初设决策，**需求边界不再扩大**。② ★ 本版为**核实补充 + 论证加固**，**方向未变**（G1–G5 / N1–N5 不动）。③ ★★ **我方核实补漏（评审未提）：新增 K13「客户区尺寸有 3 个入口」**——① 构造期 `GetClientSize`（`Win32PlatformWindow.cpp:266`）→ `RootWidget::SetSize` ② `OnResized`（`:521` → `Window.cpp:449`，**同步尺寸 + 重排布局链**，**布局几何的真正源头**）③ `WindowResizedEvent`（`WindowMessageHandler.cpp:106`）；v1.0 的 K7 只覆盖 ③ ⇒ **§4 触面清单同步改为「3 个入口」**。④ ★ **新增 §4.1「双空间共存」**：**同一个 `GetClientRect` 在平台侧须 DIP、在 `GDIBackend` 侧须物理**，两处**不得合并成一个尺寸访问器**。⑤ **R3 升级为【架构约束】**（评审：其地位高于 G1）· **Q4 增 YAGNI 护栏** · **Q6 增可测判据**（DPI 来源与翻译解耦）· **Q5 增倾向 A**，并把判据由评审的「符合 R3」**修正为**「`HitTest` 入参语义恒为 DIP」（**B 同样在平台边界内，R3 区分不了**）。⑥ **未采纳 1 处**：评审把 K7 表述为「直接透传**物理**坐标」——现状下透传的是**逻辑像素**（正是「两条错误互相抵消」的由来），K7 原文准确故保留。⑦ **索引回写**：根 `README.md` 状态行 · `docs/README.md` Phase20 标题与文档表 · 本文件 #8 行。⑧ 头部 v1.20 → **v1.21**。
- v1.20（2026-09-24）**#8 DPI 感知立项 → Phase 20**。① **需求确认 v1.0 落地**（`docs/phase20-dpi-awareness-requirements.md`）：**K1–K12 现状勘察全部带行号**——★ **K1 DIP 契约已立**（`Win32PlatformWindow.cpp:376`「换算点唯一在此——公共 API 语义恒为 DIP」）· **K3 `DipToPixels` 的 DPI 来源 = `GetDeviceCaps` 并预留 `GetDpiForWindow` 替换点** · ★★ **K5 进程未声明 DPI 感知（全库 0 命中）= 全部症状的根因** · ★★ **K6 = Phase 13 详设的 L6/D6 明写「与 DPI 感知一并闭合」**（`IsClientInteractiveAt` 的物理像素 vs `HitTest` 的 DIP 几何，`Win32PlatformWindow.cpp:423`）。② ★ **§1.3 的关键判断**：**现状不是正确，是「两条错误互相抵消」**（未声明感知 ⇒ 系统虚拟化 ⇒ `dpi` 恒 96 ⇒ `DipToPixels` 恒等 ⇒ 契约恰好与事实重合而未被检验）。③ **路线 A/B/C**：**倾向 B**（声明 Per-Monitor V2 + DIP 契约贯彻**全部平台边界**）——否决 A（unaware 下查询也是虚拟化值）与 C（只在 chrome 生效 ⇒ **契约变「半真」，比现状更糟**）。④ ★★ **§3.2 连锁关系（本阶段最重要的一条）**：**感知声明与 DIP 语义贯通必须同批**——单独声明感知会让窗口与字体在 150% 屏上**缩小 1/3**，是**纯破坏性变更**。⑤ **G1–G5**（含 **G5「100% 下逐位等价」**作为零回归护栏）· **N1–N5** 非目标 · **§4 触面清单 10 面**（★ 渲染后端**不需变**——它本就工作在最终像素）· **D0–D8** · **R1–R9** · **A1–A6**（区分自动 / 人工）· **Q1–Q6**（★ Q2「文本测量返回 DIP 还是物理」是**最实际的取舍**）。⑥ 头部 v1.19 → **v1.20**。
- v1.19（2026-09-23）**#41 关闭（Phase 19 已实现并收口）**。① **#41 标 ✅ 已实现**：`MouseEvent` 族新增「当前按下的按键位 + 修饰键」——**四工具链 `ecdi_tests` 236 全绿**（231 + 5）· 断言特征串 **11 → 11** · 公共头 **92 → 92** · **公共 API +2**（`MouseEvent::IsButtonDown` / `HasModifier`）· **8 文件改动 / 无新文件**。② ★★ **P1/P2/P3 实测回填**（详设 §6.1，独立探针）：`WM_LBUTTONDOWN` 置 `MK_LBUTTON` · `WM_LBUTTONUP` **不含**（该位已清）· **`WM_MOUSEMOVE` 按住时置位** ⇒ `IsButtonDown()` 在**按下 / 拖动 / 抬起**三阶段**全部可读**。③ ★ **给消费者的边界**（新）：**抬起那一刻该位已清** ⇒ 「刚才是否在拖」**不能**用 `IsButtonDown` 判断，须用 `GetButton()`；这也是 `ScrollBar::m_dragging` / `TextBox::m_mouseDown` 的 **D4 结论不变**的新证据。④ **配套审计升 v1.3**（`docs/framework-defect-audit.md`：**D-1 标 ✅ 已修复**）。⑤ **队列推进**：首位（①）完成，下一项 = **② DPI 感知**（= #8）。⑥ 头部 v1.18 → **v1.19**。
- v1.18（2026-09-23）**新立 #45「3D 渲染扩展边界」（优先级最低）**。① ★★ **核心决策**：**3D 不进入 ECDI Core Rendering Pipeline**——证据 = `RenderCommand.h:90-91` 的 `std::visit` 穷尽性；若 3D 命令进 variant，则 `Renderer` 必加重载、`RenderingBackend` 必含 3D 方法 ⇒ 要么 GDI 被迫实现、要么静默丢弃 ⇒ **与「3D 可选」自相矛盾** ⇒ `RenderCommand` / `PaintContext` / `CommandBuffer` / `Renderer` / `RenderingBackend` **全部不改**。② **形态** = 独立可选接口（名 `RenderingBackend3D` **为候选、未定死**；与 `RenderingBackend` **无继承**；支持者**多继承**两者）⇒ 2D 强制 / 3D 可选字面成立。③ ★ **旁路须正式承认**（属第五条通路：应用 ↔ 后端直连，不经那四层）。④ **消费形态优先级（用户拍板）**：① 视图**优先** · ② 独占窗口**有需求再考虑** · ③ 同帧合成**看情况**；★ **但 ① 的接口形状不现在定死**（`RenderToImage` 只是其一；`Image` = **CPU 侧值语义** ⇒ 高频刷新有 readback 往返代价）。⑤ ★ **决策成本不是零**：将来公开该接口即成为公共 API（名字 / 生命周期 / 资源 / 线程 / Context 归属）⇒ 现在连名字都只作候选。⑥ ★★ **重启第一问题**：3D 扩展是**框架自己负责**还是**允许外部模块接入**（决定它是高层场景接口还是 GPU Resource / Command 接口）。⑦ **命名纪律**：**无 `I` 前缀** · **`Viewport` 已被 2D 占用须换词**。⑧ **重启条件 R-1**（真实 3D 消费者且形态 ① 不足）· **R-2**（#44 的 R-5 = Linux/Android 开工顺带触发）。⑨ 头部 v1.17 → **v1.18**。
- v1.17（2026-09-23）**按用户给出的后端路线图修正 #44 的判断**。① ★ **自我修正**：原把 **M-3「submit 与 present 合一」**列为缺口——**过重**。`EndFrame` 同时承担提交与呈现，对 **GDI / GL 这类"画完即呈现"的模型是正确抽象**（GL 的 `SwapBuffers` / `eglSwapBuffers` 恰好落在这一位）；**只有 Vulkan / D3D12**（需分离 submit 与 present）才会暴露 ⇒ **降级为"Vulkan 阶段的前瞻项"**。② ★★ **新增 M-4「文本栅格化属后端职责」**：GDI 靠 `TextOutW` 由**系统**栅格化，GL / Vulkan 必须**自己栅格化字形 + 维护纹理图集** ⇒ **GPU 后端的最大一块工作量**，且与 `TextMeasurer` **成对替换**（Phase 7.1 预留的配对**方向正确**）。③ **新增 M-5（非缺口但须知）**：GPU 后端需要**攒批**——**接口不必改**（后端内部累积到 `EndFrame` 一次提交，`RenderCommand` 序列本身有序），但**后端复杂度上升**（图集 / 状态排序）。④ ★ **记入用户的后端路线图**：**Windows = GDI → Linux + Android = OpenGL / GLES → 之后学 Vulkan**；**重启条件补 R-5**（Linux / Android 适配开工即重启）；附**空壳 spike 先行**的建议（实测代替纸上分析）。⑤ 另记 **Android 的额外成本**（多点触控 ≠ 单指针鼠标 · 键码表第三份 ⇒ 大头在平台层与事件模型）。⑥ 头部版本 v1.16 → **v1.17**。

- v1.16（2026-09-23）**新立 #43 / #44（同源两条：抗锯齿与后端可替换性）**。① **#43 抗锯齿只覆盖圆角**（= 审计 **D-6**）：证据 = 勾线走 `DrawLine`（`CheckBox.cpp:99-102`）而 `GDIBackend::DrawLine` 用 GDI `MoveToEx`/`LineTo` 无 AA；★ 与 **#44 同源**——都是"**视觉语义属后端私有实现**"的侧面。② **#44 后端可替换性**（**形态记账**）：记**已就绪五项** + **缺口 M-1 / M-2 / M-3** + **重启条件 R-1..R-4** + 届时**优先加的三个位**；★ 附 **X11 选库建议**（Cairo 优于 Phase 7.1 注释里预留的 `OpenGLRenderer`）。③ ★ **同期落地一件实事**：#44 所指"覆盖度像素合成"已从 `GDIBackend` **抽为平台无关的 `Render/CoverageRaster`**（零拷贝、行为逐位不变）——**记账不是空账，第一个可复用积木已就位**。④ 头部版本 v1.15 → **v1.16**。

- v1.15（2026-09-22）**新立 §7.9「框架缺陷修复队列」+ 排期转向**。① 用户拍板「**先做缺陷修复**」——desktop demo **推迟**到框架缺失补齐之后（demo 是"用法示范"，能力未定型时写它 = 维护两份真相；ModelProbe 继续承担验证职责）。② **§7.9 建档**：**#41 鼠标事件维度补全**（**契约错误**，顺带解锁 #36）**已立项 Phase 19** · **#8 复用为 DPI 感知**（优先级提升，不新开号）· **G-2 / G-3 引用 `desktopnest-roadmap`**（不重复开号）· **新立 #42 列表/网格容器**（第二个消费者已出现，与 #33 虚拟化解耦）。③ **配套审计文档**：`docs/framework-defect-audit.md` v1.0（证据 + **该修 / 记账分界判据** + 队列顺序 + **记账不动清单 A-1..A-9** 含重启条件）。④ 头部版本 v1.14 → **v1.15**。

- v1.14（2026-09-22）**#39 关闭（Phase 18 已实现并收口）+ #40 就地解决（Phase 18.1 搁置）**。① **#39 标 ✅ 已实现**：四工具链 **231 全绿** · 断言特征串 11→11 · 公共 API +2 · A1–A7 全过；勘误 △8「6 处 → **3 处**」（详设回写 v1.2）。② **#40 记结论四条 + R3 措辞更正 + 重启条件 R-1..R-4**（layered 要求：chrome 贴边 / 内容留白 = 基本要求，露窗口底色 = 可选效果；**chrome 贴边 = 平台契约**，依据 `WM_NCHITTEST` 的全宽 caption 带）。③ **Phase 18.1 搁置**：路线 C（应用侧透明 `Panel`）已落 demo 侧、**框架零改动**；路线 A 因与 C 等价且引入近义词而**降级为记账**。④ 头部版本 v1.13 → **v1.14**。

- v1.13（2026-09-22）**新立 §7.8（#40）+ Phase 18.1 立项**：由 **Phase 18 A6 目视实测**派生——root 级留白必然连带内缩 `CaptionBar`（`padding` 布局级单一 int，对全部子一律），而 Phase 18 只解决了「清屏白」这一半。① **新增 §7.8「同一父布局下的留白归属」条目 #40**（含**关键事实**：`Panel` 默认透明 = 官方「隐形布局容器」语义 ⇒ **应用侧零改动组装**即可达成同一视觉，Phase 17 判其代价过高所依据的「`SetSize` override 同步」已失效）。② **#39 的实现状态**（Phase 18 批 A/B 已落盘 · `ecdi_tests` **231 全绿** 四工具链 · A6 接线已落盘）**暂不在此回写**，随 Phase 18 的 **A7 收口**统一落定（届时 #39 标 ✅ 已实现）；本条目仅记明「**A6 的视觉结论 = 本条目 **#40** 的来源**」。③ 头部版本 v1.12 → **v1.13**。

- v1.12（2026-09-21）**#39 立项 → Phase 18**：**§7.7 标题标注「已立项 Phase 18」**，#39 备注追加重启记录；需求确认 **v1.0** 落地（`docs/phase18-window-background-requirements.md`）——**§1.2 现状勘察 K1–K8 全部带行号实测**（K1 `GDIBackend.cpp:261` 硬编码 `WHITE_BRUSH` · K2 `RenderingBackend` 无背景入口 · K3 `Window` 无背景 API · K4 root 是裸 `Widget` ⇒ 「客户区底色」**无任何可配置入口**；K8 记明库内 `RecordingBackend` 被 12 个测试文件依赖）；**§1.3 锁定 Phase 4 四层不变量**（清屏属能力层 / 颜色属决策层）；技术路线三案（**A 倾向**）；R1–R6 · D1–D7 · N1–N6 · Q1–Q4。头部版本 v1.11 → **v1.12**。

- v1.11（2026-09-21）**#38 关闭（已实现）+ 新立 §7.7（#39）**。① **#38 标 ✅ 已实现——Phase 17**（批 A/B + A6 落盘；`ecdi_tests` **226 全绿**；断言特征串 10 → **11**；A1–A6 实测通过；文档详设 **v1.2** / 需求 **v1.2**）。② ★ **实施期偏离已记档**：留白由计划的 root 一级改为 **page 一级**（root 无背景 ⇒ 露 Backend 白 + 连带内缩标题栏）。③ **新立 §7.7「根/窗口背景能力」（条目 #39）**——由该偏离暴露出的框架侧缺失能力，重启条件 = 「真·窗口四周留白」需求出现。④ 头部版本 v1.10 → **v1.11**。

- v1.10（2026-09-20）**#38 状态推进：立项 → 三阶段文档齐备**。原 v1.9 记「倾向方案 B」，本条记录实际推进：① 用户拍板**方案 B（框架侧）**；② 需求确认 **v1.0** 落地（K1–K10 现状勘察带行号 · R1–R10 · D0–D5 · N1–N7）；③ 初步设计 **v1.1** 落地（两轮外部评审：第二轮指出 **T17-5 判据错误**——`remaining` 是「给 stretch 子的可用空间」而非「所有子主轴尺寸总和」，已修正；并冻结 **O1** 断言形态）；④ 详细设计 **v1.0** 落地（`△1–△8` 行级 diff · 契约 C1–C6 · 测试 T17-1..T17-8 · 验收 A1–A7 · 局限 L1–L5）。**实现尚未开始**；头部版本 v1.9 → **v1.10**。
  - **同日后续**：第三轮详设评审判「**🟢 可进入实现**」（无阻塞项）。采纳其 3 项修正——需求稿 §6 用例表按详设定稿口径**回填为 v1.1**（`PaddingEmptyAndSingle` → `PaddingOverflow`；`PaddingHorizontalSymmetric` 取消、位置给新增的 `PaddingIdempotent`；「空容器」降级为结构性保证不单设用例）；**A2（断言特征串）与 T17-8 的概念边界**（A2 是**结构性验收**，不属 T17-8）；**A1 的 226 标为设计目标**（实施后以实测回填）。另补盯防项「`FRAMEWORK_ASSERT` 必须判**形参**、不得改判 `m_padding`」。详设 → **v1.1**（新增 §5.5 覆盖项映射）。
- v1.9（2026-09-19）新增 **§7.6 布局内边距**（条目 **#38**）——用户观察 ModelProbe 内容贴边触发；已核实「零边距是结构性的」（`VerticalLayout::Arrange` 跨轴恒 0 + `remaining` 无 inset）；倾向 **方案 B**（`LinearLayout` 加 `padding` 形参，`Arrange` 改 4 处，root 一处即全局生效）；备选 A（demo 侧包裹 + `SetSize` 同步）一并记录。**并修正头部状态行**——原写 v1.2（2026-09-11）而 §8 已记到 v1.8，属状态漂移（skill 条 30 同族），已同步为 v1.9。
- v1.8（2026-09-18）Phase 15 需求稿 v1.1 回写同步：**修正 §7.5 编号撞号（v1.0 引入）**——原用 28–32，与 §1.2 的 `28`、§7 的 `29` 重复，统一顺延为 **30–34**；条 30 补 **`CaptionBar` 窄窗越界实例**（窗口宽 < 138px，既有缺陷）；新增条 35 嵌套滚动停递（D7 v1.1 降级）· 条 36 横向滚轮（K15/K16 实测：平台无翻译 + Event 无修饰键）· 条 37 负向内容（D6 extent 钳 0）。
- v1.7（2026-09-17）新增 §7.5 Phase 15 延期项（①②③④⑤ 五条）：K4 命中约束通用化（以 `ClipsChildren()` 门控局部收口）· 焦点滚入 · 平滑/惯性 · 虚拟化 · TextBox 横向滚动条（Phase 9.5 R1 记账的边界澄清——仅指 TextBox，非容器级）。
- v1.0（2026-08-15）总表定稿：全部延期项分组到阶段（7/7.5/8/8.5/9/9.5）；确认 SetFont 入 8.5、21-25 入 9.5。
- v1.1（2026-08-15）**Phase 7 拆 7.1/7.2**（用户决策）：测试体系入 7.2（新增 #28——5.5.2 P8 承诺的 Selection 单元测试补测；Phase 10 转库前测试保障）。
- v1.2（2026-09-11）新增 **§7 Phase 8.6 渲染抗锯齿**（条目 #29）——把 `phase9.5-alpha-primitive-detailed-design.md` §4 的「抗锯齿升级」正式纳入总表并立项（该延期项此前只存在于 9.5 详设清单，总表零记录，属登记遗漏）。本次**仅补登记 AA 一项**；9.6 / 9.7 / 9.8 / 10 / 11 / 12 的延期项批量回填仍待单独授权（见头部已知缺口声明）。
