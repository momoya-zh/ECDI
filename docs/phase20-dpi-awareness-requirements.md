# Phase 20 · DPI 感知 —— 需求确认（v1.0）

> 来源：`roadmap-deferred.md` **§7.9 #8**（= §1.1 条 **8**「DPI 感知评估」的优先级提升）· `docs/framework-defect-audit.md` **§4 D-2**（= 队列 **②**）
> 状态：**v1.0**（2026-09-24）——**待评审**（评审通过后方可进初步设计）
> 前置输入：**Phase 13 详设 §12 的 D6 / L6**（明写「与 DPI 感知一并闭合」）· **Phase 16 spike §7.1**（跨屏 ×0.8 实测）

---

## 1. 动机

### 1.1 一句话

**框架早已在契约上声明「公共 API 语义恒为 DIP」，但因为进程未声明 DPI 感知、`dpi` 恒 96，这条契约从未被检验过**——本阶段的任务是**让它真正生效**，而不是引入一个新概念。

### 1.2 现状勘察（K1–K12，全部带行号实测）

| # | 事实 | 证据 |
|---|---|---|
| **K1** | ★ **DIP 契约已立**：`WM_NCHITTEST` 内的注释明写「**D-DPI-1：换算点唯一在此——公共 API 语义恒为 DIP**」 | `Win32PlatformWindow.cpp:376` |
| **K2** | `DipToPixels(int dip, HWND)` 是**全库唯一**的 DIP→像素换算函数；公式 `px = (dip * dpi + 48) / 96`（整数 half-up，**dpi == 96 时恒等于 dip**） | `Win32PlatformWindow.cpp:1322`（定义）· `:1341-1345`（公式） |
| **K3** | ★★ **DPI 来源 = `GetDeviceCaps(hdc, LOGPIXELSX)`**（窗口 DC）；注释自陈「**当前项目未声明 PerMonitorV2 → 系统 DPI 虚拟化下 dpi 恒 96 → 现状 1:1**」，并**预留替换点** `GetDeviceCaps → GetDpiForWindow` | `Win32PlatformWindow.cpp:1339` · `:1324-1330` |
| **K4** | `DipToPixels` **只有 2 个调用点**，都在 `WM_NCHITTEST` 里：`resizeInset` 与 `captionHeight` ⇒ **DIP 语义目前只覆盖 chrome 两项** | `:377` · `:379` |
| **K5** | ★ **进程未声明 DPI 感知**：全库 `SetProcessDpiAwareness` / `SetProcessDPIAware` / `DPI_AWARENESS_CONTEXT` **0 命中**（仅 `.workbuddy/spike/` 里的探针显式调过，与本仓库无关）· **无 manifest**（`examples/ModelProbe/ModelProbe.rc.in` 里无 manifest 项） | grep 全库 · `ModelProbe.rc.in` |
| **K6** | ★★ **`WM_NCHITTEST` 与 widget 几何的错位已在 Phase 13 记录并留待闭合**：`IsClientInteractiveAt(x, y)` 拿到**物理像素**，`HitTest` 用 **DIP 几何** ⇒「**两者仅在 100% DPI 下重合**」，处置写的是「**与「DPI 感知」技术债一并闭合**」 | `phase13-captionbar-detailed-design.md:795`（**L6**）· `:1051`（**D6**）· 错位点 `Win32PlatformWindow.cpp:423` |
| **K7** | 鼠标 / 尺寸事件坐标**直接透传、无换算**：`WM_MOUSEMOVE` 取 `GET_X_LPARAM/GET_Y_LPARAM`；`WM_SIZE` 取 `LOWORD/HIWORD(lParam)` | `WindowMessageHandler.cpp:92-106` · `:104-118` |
| **K8** | ★ **`Font::size` 的语义是「GDI 像素高度」**（明确写着的），不是 DIP | `Core/Font.h:11` |
| **K9** | 字体实例化**直接按像素用**：`lf.lfHeight = -lround(font.size)`（`MM_TEXT` 下 `lfHeight` 即像素） | `GDIBackend.cpp:771` |
| **K10** | 文本测量走 GDI、返回**像素**：`GetTextExtentPoint32W` / `GetTextMetricsW`（`tmHeight`） | `GDITextMeasurer.cpp:76` · `:107-109` |
| **K11** | IME 双通道用**框架坐标**（隐含 DIP 假设）：`SetCaretPos(geometry.rect.x, y)` + `COMPOSITIONFORM` / `CANDIDATEFORM` | `Win32PlatformWindow.cpp:683` · `:708-714` · `:718` |
| **K12** | `MONITORINFO` **仅用于最大化修正**，框架**无面向应用的屏幕 / DPI 查询 API**；`PlatformWindow` 接口里**没有任何 DPI 方法** | `:1305` · `include/ECDI/Platform/PlatformWindow.h:31-134` |

### 1.3 为什么现状「看起来能工作」

**K5 是全部原因**：进程未声明感知 ⇒ **系统对整个窗口做 DPI 虚拟化** ⇒ 框架看到的一切（客户区尺寸、鼠标坐标、`GetDeviceCaps` 返回值）都落在系统 DPI 空间的**逻辑像素**上 ⇒ `dpi` 恒 96 ⇒ K2 的公式**恒等** ⇒ 「公共 API = DIP」这条契约**恰好与事实重合**。

⇒ **结论**：现状不是「正确」，是「**两条错误互相抵消**」。三条已知症状都由此而来：

| 症状 | 来源 | 证据 |
|---|---|---|
| **跨屏后矩形 ×0.8**（420×280 → 336×224） | 跨 DPI 空间的坐标换算；已排除 `GetDpiForWindow`（全程 96）/ `WM_DPICHANGED`（未收到）/ 感知级别（=1）⇒ `0.8 = 96/120` 逐位吻合 | `desktopnest-roadmap.md:253`（Phase 16 spike §7.1） |
| **`WM_NCHITTEST` 委托与 widget 几何错位** | K6（Phase 13 实测） | 同上 |
| **无法查询真实 DPI / 屏幕信息** | K12 ⇒ 应用**想自己适配也拿不到数据** | 同上 |

---

## 2. 目标与非目标

### 2.1 目标（本阶段要达成的）

| # | 目标 |
|---|---|
| **G1** | **进程 DPI 感知**：从「隐式 unaware」升到 **Per-Monitor V2** |
| **G2** | ★ **让既有 DIP 契约真正生效**：公共 API 的尺寸 / 坐标语义**恒为 DIP**，换算**只发生在平台边界**（把 K1 的「换算点唯一」原则从 NCHITTEST 扩展到全部边界） |
| **G3** | **跨屏一致性**：窗口移到另一 DPI 的显示器后，尺寸 / 内容 / 命中全部正确（含 `WM_DPICHANGED`） |
| **G4** | **补查询能力**：面向应用的**屏幕 / DPI 查询 API**（K12 的空缺） |
| **G5** | ★ **100% DPI 下逐位等价**：`dpi == 96 ⇒ px == dip`（K2 公式已保证）⇒ **既有测试与视觉零回归** |

### 2.2 非目标（明确不做）

| # | 非目标 | 理由 |
|---|---|---|
| **N1** | **不做 DPI 无关的「矢量字体缩放」** | `Font::size` 语义变成 DIP 后，字体仍由 GDI 按物理高度栅格化；本阶段不改渲染管线 |
| **N2** | **不做主题 / 布局的 DPI 自适应策略**（如「小屏自动紧凑」） | 那是响应式设计，不是 DPI 感知 |
| **N3** | **不做非 Windows 平台的 DPI 抽象** | 与 #44 后端可替换性解耦；X11/Android 各自的缩放模型不同，届时另议 |
| **N4** | **不重构 `ScrollBar` / `TextBox` 的坐标计算** | 它们拿的是框架坐标（已 DIP），**只要边界换算正确，它们不用改** |
| **N5** | 不做**每屏独立缩放系数**的用户可配（如「某屏设 110%」） | 系统级能力，跟随系统 |

---

## 3. 技术路线

### 3.1 三条候选

| 路线 | 内容 | 代价 | 判定 |
|---|---|---|---|
| **A** | **保持 unaware**，只补查询 API | 小 | ❌ **否**——unaware 下查询到的也是虚拟化值（K5），解决不了 K6/跨屏；且 `roadmap` 已把目标定为 V2 |
| **B** | ★ **声明 Per-Monitor V2 + 让既有 DIP 契约生效**（G2 贯通全部平台边界） | **中～大**（触面广，见 §4） | ✅ **倾向**——与 K1 既有契约同向，且 G5 提供零回归护栏 |
| **C** | 声明 V2 但**只在 chrome 生效**（DIP 语义不向外扩） | 小 | ❌ **否**——会让「公共 API = DIP」的契约**变成半真**（chrome 是 DIP、窗口尺寸不是），比现状更糟 |

### 3.2 ★ 一个必须先说清的连锁关系（决定分批方式）

**V2 一旦声明，系统停止虚拟化 ⇒ 坐标变物理像素。** 此时若不同时把「窗口尺寸的 DIP 语义」补上：

- `Application::Create(title, 800, 600)` 的 `800` 会被当作**物理像素**直接交给 `CreateWindowEx` ⇒ **150% 屏上窗口视觉缩小 1/3**（`800 DIP` 应变 `1200 px`）；
- 同理**字体**（K9）在 150% 屏上会变小 1/3（`14 px` 应为 `21 px`）。

⇒ **「声明感知」与「DIP 语义贯通」不能分两批做**——单独声明感知是一个**纯破坏性**变更。这两件事**必须在同一批内完成**。

---

## 4. 触面清单（G2 的实际工作量）

| 面 | 现状（K#） | V2 后需要 | 方向 |
|---|---|---|---|
| **窗口创建尺寸** | 直传 `CreateWindowEx`（隐式逻辑） | **DIP → 物理** | 入 |
| **chrome**（caption / resizeInset） | ✅ 已 DIP（K1/K4） | DPI 来源换成 `GetDpiForWindow`（K3 预留点） | 已就位 |
| **鼠标事件坐标** | 透传（K7） | **物理 → DIP** | 出 |
| **窗口尺寸事件** | 透传（K7） | **物理 → DIP** | 出 |
| **`WM_NCHITTEST` 委托** | ★ **错位**（K6） | 统一到 DIP 后**自动闭合 L6** | 出 |
| **字体实例化** | 按像素（K9） | **DIP → 物理**（且**字体缓存 key 必须含 DPI**——否则跨屏复用错字体） | 入 |
| **文本测量** | GDI 像素（K10） | 与 `Font` 语义**同源** | 定 |
| **IME 双通道** | 框架坐标（K11） | **DIP → 物理** | 入 |
| **渲染后端**（`GDIBackend` 的 DIB / `GetClientRect`） | 物理 | ✅ **不需变**（后端本来就工作在最终像素） | — |
| **公共 API 缺口** | 无查询（K12） | **新增屏幕 / DPI 查询** | 新 |

★ **一句可判的总结**：**公共 API 与框架内部（Widget / 布局 / 事件）= DIP；平台边界 = 物理；渲染后端 = 物理。** 换算**只在边界的两个方向发生**。

---

## 5. 决策点（D0–D8）

| # | 问题 | 倾向 |
|---|---|---|
| **D0** | **分几批做？** | **一批**：感知声明与 DIP 贯通**必须同时**（§3.2）；但**测试与收口**可分步 |
| **D1** | **换算的唯一入口怎么保证？** | 把 K2 的 `DipToPixels` **泛化**为平台侧一对工具（`DipToPixels` / `PixelsToDip`），并**规定所有边界调用它**——沿用 K1 的「换算点唯一」原则 |
| **D2** | **DPI 来源** | **`GetDpiForWindow`**（per-window）——与 K1/K3 既有理由一致（「跟随窗口比跟随鼠标更一致」） |
| **D3** | **`WM_DPICHANGED` 怎么处理？** | 采纳系统建议矩形（`lParam` 是 `RECT*`）+ 触发重布局 + **清字体缓存** |
| **D4** | **`Font::size` 的语义** | 从「**像素高度**」改为「**DIP 高度**」（K8）——★ **数值不变**，故 100% 下逐位等价（G5）；但这是**公共语义变更**，须写进文档与头注释 |
| **D5** | **文本测量返回 DIP 还是物理？** | ★ **留给初步设计**（见 Q2：DIP ⇒ 浮点误差 vs 物理 ⇒ 布局混合） |
| **D6** | **`WindowResizedEvent` 报什么？** | **DIP**（与公共 API 一致） |
| **D7** | **感知声明放在哪？**（框架是静态库，manifest 得由应用提供） | **两案并存**：① 提供 manifest 片段 / CMake 注入供应用使用 ② `Application::Create` 早期运行时调用 `SetProcessDpiAwarenessContext`（**必须在任何窗口创建前**）。★ **取舍留给初步设计**（Q3） |
| **D8** | **查询 API 的形状** | 候选：`Window` 上的 `GetDpiScale()`（float，窗口级）+ 独立的应用级屏幕查询。★ **形状留给初步设计**（Q4） |

---

## 6. 需求条目（R1–R9）

| # | 需求 |
|---|---|
| **R1** | 进程 DPI 感知 = **Per-Monitor V2**；且**声明时机早于任何窗口创建** |
| **R2** | 公共 API 的尺寸 / 坐标语义 = **DIP**（把 K1 的既有契约**贯彻到全部边界**） |
| **R3** | 换算**只发生在平台边界**；框架内部（Widget / 布局 / 事件 / 命令缓冲）**不感知 DPI** |
| **R4** | `WM_DPICHANGED` 后：几何正确 + 重布局 + 字体缓存失效 |
| **R5** | 字体与文本测量的语义与 `Font::size` **同源**（D4 / Q2） |
| **R6** | **零破坏**：100% DPI 下**逐位等价**（G5）；既有 236 用例全绿 |
| **R7** | 新增**屏幕 / DPI 查询 API**（K12 空缺）——★ 形状与范围待初设（Q4） |
| **R8** | 既有记账项 **L6 / D6（K6）在本阶段闭合**，并在 Phase 13 文档回填状态 |
| **R9** | 可测性：**纯函数（换算）与事件翻译必须自动可测**；跨屏行为允许人工验收（须写明） |

---

## 7. 验收判据（A1–A6）

| # | 判据 | 方式 |
|---|---|---|
| **A1** | 换算函数在 `dpi = 96 / 120 / 144 / 192` 下的取值与公式逐位一致；`dpi = 96` 时**恒等** | 自动（纯函数） |
| **A2** | 事件翻译：给定物理坐标 + DPI ⇒ 事件坐标 = 预期 DIP | 自动（`FakeHost` + `Handle()` 真翻译路径，沿用 Phase 19 先例） |
| **A3** | **零回归**：100% DPI 下既有 **236** 用例全绿（须写明断言是否启用） | 自动（四工具链） |
| **A4** | 感知声明生效：运行时查询进程感知级别 = **Per-Monitor V2** | 自动（运行时查询） |
| **A5** | 视觉：125% / 150% 屏上**窗口物理尺寸按比例放大、内容不缩小**；拖动 / 双击最大化 / Aero Snap 正常 | **人工**（与 Phase 16 spike 同款目视） |
| **A6** | 跨屏：窗口移到另一 DPI 显示器后，尺寸与命中正确（K6 错位消失） | **人工**（双屏环境） |

---

## 8. 留给初步设计的问题（Q1–Q6）

| # | 问题 |
|---|---|
| **Q1** | **DIP 语义的边界画在哪**：`Application::Create(title, w, h)` 之后，`Widget::SetSize` / `Layout` 的参数是否也一律 DIP？（倾向：**是**——但需确认无「物理量」混入，例如 DIB 尺寸） |
| **Q2** | ★ **文本测量的单位**：返回 DIP（浮点，会有累积误差）还是物理（整数，但布局须混合单位）？**这是本阶段最实际的取舍** |
| **Q3** | **感知声明的落点**：manifest（应用侧 / CMake 注入）vs 运行时 API（框架侧）——**两者是否都要**？若走运行时，`Application::Create` 是否是最早的可靠时机？ |
| **Q4** | **查询 API 的形状**：窗口级 `GetDpiScale()` 够不够？要不要屏幕枚举（多显示器信息）？命名如何避免与 `Viewport` 撞概念 |
| **Q5** | **`WM_NCHITTEST` 的换算方向**：该消息给的是**物理像素**，而 K1 说「换算点唯一在此」——是把入参**换成 DIP** 再比对，还是把 widget 几何**换成物理**再比对？两者对 L6 的闭合效果相同，**但影响该函数的契约表述** |
| **Q6** | **测试如何伪造 DPI**：换算函数可注入 DPI；但**平台层的事件翻译**（`WindowMessageHandler`）怎么在无头测试里注入非 96 的 DPI？沿用 Phase 19 的 `FakeHost` 路径是否足够 |

---

## 9. 修订记录

- **v1.0**（2026-09-24）初稿。**输入**：`roadmap-deferred.md` #8（D-2）+ **Phase 13 详设 §12 的 D6/L6**（明写「与 DPI 感知一并闭合」）+ **Phase 16 spike §7.1 的 ×0.8 实测**。**内容**：K1–K12 现状勘察（全部带行号）· **§1.3 指出「现状不是正确，是两条错误互相抵消」**（K5 是全部原因）· G1–G5（含 **G5 逐位等价**护栏）· N1–N5 非目标 · 路线 A/B/C（**倾向 B**，并指出 **C 会让契约「半真」比现状更糟**）· ★ **§3.2 的连锁关系**（感知声明与 DIP 贯通**必须同批**，否则是纯破坏性变更）· §4 触面清单 · D0–D8 · R1–R9 · A1–A6（区分自动 / 人工）· Q1–Q6（★ Q2 测量单位是最实际的取舍）。
